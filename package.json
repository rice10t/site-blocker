{
  "name": "site-bloker",
  "version": "0.1.0",
  "scripts": {
    "lint": "web-ext lint",
    "build": "web-ext build --overwrite-dest",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "prettier": "^2.8.4",
    "web-ext": "^7.5.0"
  }
}
